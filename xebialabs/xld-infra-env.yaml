# Provision GCP GKE cluster
apiVersion: xl-deploy/v1
kind: Infrastructure
spec:
- name: deepu-test-gke
  type: core.Directory
  children:
  - name: gcp-terraform
    type: overthere.LocalHost
    os: UNIX
    children:
    - name: terraform-client
      type: terraform.TerraformClient
      path: '/usr/local/bin' # for local installation
      workingDirectory: '/tmp/deepu-test-gke' # for local installation

---
apiVersion: xl-deploy/v1
kind: Environments
spec:
- name: deepu-test-gke
  type: core.Directory
  children:
  - name: gcp-terraform
    type: udm.Environment
    members:
    - Infrastructure/deepu-test-gke/gcp-terraform
    - Infrastructure/deepu-test-gke/gcp-terraform/terraform-client